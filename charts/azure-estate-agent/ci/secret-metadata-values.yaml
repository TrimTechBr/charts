# The optional Secret grant, and the inline key path. Both are exercised here so
# a change to either is rendered before it reaches anyone.
portal:
  url: https://estate.ci.example
  clusterId: /subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/rg/providers/Microsoft.ContainerService/managedClusters/aks-ci
  apiKey: ci-only-not-a-real-key
rbac:
  secretMetadata: true
listener:
  enabled: false
